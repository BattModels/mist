[project]
name = "olfaction"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.10, <3.14"
dependencies = [
    "electrolyte_fm",
    "matplotlib>=3.10.8",
    "pubchempy>=1.0.5",
]

[tool.uv.sources]
electrolyte_fm = { path = "../../", editable = true }
