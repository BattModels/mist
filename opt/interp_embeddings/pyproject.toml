[project]
name = "interp-embeddings"
version = "0.1.0"
description = "Interpretibility of pretrained model embeddings."
readme = "README.md"
requires-python = ">=3.10, <3.14"
dependencies = [
    "matplotlib>=3.10.1",
]
