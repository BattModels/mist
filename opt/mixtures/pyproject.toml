[project]
name = "mixtures"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.10, <3.14"
dependencies = [
    "accelerate>=1.8.1",
    "dataset>=1.6.2",
    "electrolyte_fm",
    "transformers>=4.53.2",
]

[tool.uv.sources]
electrolyte_fm = { path = "../../", editable = true }

[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "python"}

[tool.setuptools.packages.find]
where = ["python"]
