# Scripts for packaging up MIST
Mainly for bundling up the trained models for distribution, and not for external use

```shell
source activate
python opt/package --help
```

## To Do:
- License
- Model Card (Current version is sparse)
- Demo code for pretrained models
